extends ../shared/template

block content
  .row
    .small-12.columns
      #container
        if user
          #tabWrap
            h1= user.name
            h2= user.email
            h3#progressPics: a(href='/user/progress') Progress Pics
            #wodDB
